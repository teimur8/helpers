
### trim

```js
[...this.tags, this.newTag] // return new merged array
tags.filter(t => t !== tag)) // delete item from array
tags.slice(0, -1) // delete last elemetn from array
```

```php
substr('hello', 0 , -1) // hell
substr('hello', -1 , 1) // o
```